## VAGRANT
###Provisioning VM
`vagrant up`

### Access vm
`vagrant ssh` 

## ANSIBLE
`cd /vagrant/ansible`
### Create machine
`ansible-playbook deploy.yml`

### Create machine
`ansible-playbook destroy.yml`
